{{- if and .Values.enabled .Values.lokiOperator.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: openshift-operators-redhat
  labels:
    app: {{ template "logginghelm.name" . }}
    chart: {{ template "logginghelm.chart" . }}
    release: {{ .Release.Name }}
    openshift.io/cluster-monitoring: "true"
spec: {}
{{- end }}
