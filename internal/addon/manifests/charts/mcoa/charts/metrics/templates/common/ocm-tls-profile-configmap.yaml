{{- if and .Values.tlsProfileEnabled (or .Values.platformEnabled .Values.userWorkloadsEnabled) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: ocm-tls-profile
  namespace: open-cluster-management-agent
{{- end }}
